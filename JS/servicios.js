const scannerButton = document.querySelector("scanner");
const diagnosticoButton = document.querySelector("diganostico");
const mantenimientoButton = document.querySelector("mantenimiento");
const otrosServiButton= document.querySelector("otros-servi");

function cambiar() {
    if(scannerButton == onclick) {
        document.getElementById
    }
}